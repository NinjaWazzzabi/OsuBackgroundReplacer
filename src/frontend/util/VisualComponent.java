package frontend.util;

import javafx.scene.Parent;

/**
 * Interface for classes that have a {@link javafx.scene.Parent} that can be accessed.
 */
public interface VisualComponent {
    Parent getVisualComponent();
}
